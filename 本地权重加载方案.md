# 预训练权重本地加载方案

## 问题原因
项目在服务器上运行时因网络限制无法下载预训练权重，需要将权重文件下载到本地后再加载。

## 解决方案

### 步骤1：下载预训练权重
在有网络的环境中，从以下URL下载所需的预训练权重文件：

```python
# 来自 resnet.py 中的 model_urls
model_urls = {
    'resnet18': 'https://download.pytorch.org/models/resnet18-5c106cde.pth',
    'resnet34': 'https://download.pytorch.org/models/resnet34-333f7ec4.pth',
    'resnet50': 'https://download.pytorch.org/models/resnet50-19c8e357.pth',
    'resnet101': 'https://download.pytorch.org/models/resnet101-5d3b4d8f.pth',
    'resnet152': 'https://download.pytorch.org/models/resnet152-b121ed2d.pth',
    'resnext50_32x4d': 'https://download.pytorch.org/models/resnext50_32x4d-7cdf4587.pth',
    'resnext101_32x8d': 'https://download.pytorch.org/models/resnext101_32x8d-8ba56ff5.pth',
    'wide_resnet50_2': 'https://download.pytorch.org/models/wide_resnet50_2-95faca4d.pth',
    'wide_resnet101_2': 'https://download.pytorch.org/models/wide_resnet101_2-32ee1156.pth',
}
```

### 步骤2：创建权重存放目录
在项目根目录创建一个名为 `pretrained` 的文件夹，将下载的权重文件放入其中。

### 步骤3：修改代码支持本地加载

#### 修改 `resnet.py` 文件
在 `resnet.py` 中添加本地权重加载逻辑：

```python
import os
import torch
import torch.nn as nn
from torch.hub import load_state_dict_from_url

# ... 其他现有代码 ...

def _resnet(arch, block, layers, pretrained, progress, **kwargs):
    model = ResNet(block, layers, **kwargs)
    if pretrained:
        # 加载本地预训练权重（不再从网络下载）
        filename = model_urls[arch].split('/')[-1]  # 从URL中提取文件名
        weight_path = os.path.join('pretrained', filename)  # 本地权重路径
        
        # 加载权重文件
        state_dict = torch.load(weight_path, map_location=torch.device('cpu'))
        model.load_state_dict(state_dict)
    return model

# ... 其他现有代码 ...
```

#### 注意事项
如果您使用了 `networks.py` 中的 `ConvNeXt_Backbone` 类（该类也使用了预训练权重），您需要为其单独下载权重并修改加载逻辑。

### 步骤4：运行项目
将修改后的代码和 `pretrained` 文件夹一起部署到无网络服务器上，即可正常运行项目。